# Brain Dump Triage: 2026-07-24 — Web mobile UX + AI onboarding/insights

**Source**: inline (voice, cleaned) — 3 пронумерованных пункта + ops-заметка
**Triaged**: 2026-07-24T00:00:00Z

## Summary
Дамп в основном про две вещи: (1) ergonomics веб-фронта на телефоне (PWA/mobile-редизайн, особенно редактор категорий) и (2) две новые AI-фичи — разговорный AI-онбординг для генерации категорий голосом и голосовые AI-инсайты/саммари. Часть UX-квикфиксов из пункта 1 уже отгружена в PHASE-01/35. Обе AI-фичи упираются в общий вопрос: открывать ли PHASE-02 сейчас (не закончив нативный iOS PHASE-01) и делить ли между ними единый слой «voice → text». Рекомендуемый первый шаг — не код, а pre-decision о фазах и об общей STT-инфраструктуре; из строимого первым логично взять mobile/PWA-редизайн, т.к. он не зависит от AI и не требует открытия новой фазы.

## Concerns

### 1. Mobile/PWA-редизайн веб-приложения (in implementation)
- **Type**: design-brief (ведёт к PRD на редизайн layout)
- **Scope**: medium
- **Needs /grill-me**: yes (объём «переосмыслить, где что живёт» размыт — нужно сузить до конкретного layout-контракта до кода)
- **Blocks**: none
- **Blocked by**: none

**Already decided** (from dump):
- Веб-фронт используется с телефона; текущий путь Safari → Add to Home Screen неудобен — целимся в нормальный PWA/mobile-опыт.
- Редактор категории должен открываться центрированно, без длинного скролла; карточки категорий должны разумно масштабироваться/зумиться на мобиле.
- Квикфиксы (add-field внизу, sticky Cancel/Update footer, list/card toggle, одно базовое поле при создании) уже отгружены в PHASE-01/35 — в этот concern НЕ входят.

**Open questions** (need answers before grilling):
- Это полноценный PWA (manifest, service worker, offline, installable) или только адаптивный mobile-layout без PWA-инфраструктуры?
- Скоуп «переосмыслить, где всё живёт» — это редизайн информационной архитектуры/навигации целиком или только редактор категорий + масштабирование карточек?
- Веб остаётся first-class на телефоне параллельно с нативным iOS (PHASE-01), или это временный мост до iOS? Влияет на объём инвестиций.

**Suggested entrypoint**:
```
/grill-me Свести mobile/PWA-редизайн веб-фронта к конкретному layout-контракту: границы скоупа (PWA-инфра vs адаптив), центрированный редактор категорий, масштабирование карточек. Исключить уже отгруженные квикфиксы PHASE-01/35.
```

### 2. AI-онбординг: генерация категорий голосом (Has Issues)
- **Type**: PRD + ADR (новая фича + арх-решение по LLM-оркестрации/STT)
- **Scope**: large
- **Needs /grill-me**: yes (новая фича, пересекает LLM-слой, голос, запись user-данных)
- **Blocks**: none
- **Blocked by**: pre-decision о PHASE-02 (см. Friction); частично зависит от общего STT-решения с concern 3
- **Blocked by**: none (технически), но политически — от решения «открыть PHASE-02»

**Already decided** (from dump):
- Разговорный интерфейс в стиле обычного чата с Claude; пользователь голосом/диктовкой описывает свою жизнь → AI создаёт нужные категории.
- Это «отдельный backend-блок» — то есть LLM-оркестрация, а не размазанная логика.
- Категории — user-специфичные данные → Postgres (по §4 CLAUDE.md), не RAG.
- LLM вызывается только через изолированный orchestration-слой (существует `app/llm/`), не напрямую из бизнес-сервисов.

**Open questions** (need answers before grilling):
- Голосовой ввод — это web, iOS или оба? (диктует, где живёт STT и UI онбординга.)
- STT: Whisper (self-host/OpenAI) vs облачный STT vs нативный on-device (iOS Speech)? Общий ли это компонент с concern 3?
- Многоходовой диалог (чат с уточнениями) или однократная диктовка → батч-генерация категорий? Разный объём оркестрации.
- Требует ли фича формально открыть PHASE-02, или онбординг — часть PHASE-01?

**Suggested entrypoint**:
```
/grill-me AI-онбординг: диктовка → генерация категорий. Зафиксировать платформу ввода (web/iOS), STT-бэкенд, диалоговую модель (multi-turn vs one-shot) и границу LLM-orchestration-слоя. Категории → Postgres.
```

### 3. Голосовые AI-инсайты / саммари
- **Type**: PRD (расширение существующего insights-пути) + ADR по STT (если общий)
- **Scope**: medium (есть база `app/llm/` + insights API/CRUD/model, не с нуля)
- **Needs /grill-me**: yes (новый вход — голос/диктовка summary — поверх текущего period-context insights)
- **Blocks**: none
- **Blocked by**: pre-decision о PHASE-02; общий STT-выбор с concern 2

**Already decided** (from dump):
- Продиктовать summary голосом → получить AI-саммари/инсайты. Фича давно планировалась, но её нет.
- Ложится на существующий путь: `app/llm/{client,context,prompts}`, `app/api/insights.py`, `app/crud/insight.py`, `app/models/ai_report.py` (уже есть текстовые period-инсайты) и на roadmap PHASE-02.

**Open questions** (need answers before grilling):
- Голосовой ввод — это новый источник контекста (диктуемый summary) в дополнение к текущему `build_period_context`, или замена? Как хранить транскрипт?
- STT — тот же компонент, что в concern 2? (сильно влияет на порядок работ.)
- Web, iOS или оба для голосового ввода инсайтов?
- Это переиспользует `AIReport`/`InsightResponse` или нужен новый тип отчёта?

**Suggested entrypoint**:
```
/grill-me Голосовые AI-инсайты: диктовка summary → LLM-инсайт поверх существующего app/llm + insights API. Определить STT-бэкенд (общий с онбордингом?), платформу ввода, и переиспользование AIReport vs новый тип.
```

## Suggested Order

1. **PRE-DECISION (сначала, до любого grill AI-фич)**: решить (а) открывать ли PHASE-02 сейчас или доделать iOS PHASE-01, и (б) единый ли слой «voice → text» под concern 2 и 3. Это не тикет — это развилка пользователя.
2. **NOW / независимо**: concern 1 (mobile/PWA-редизайн) — не зависит от AI, не требует открытия PHASE-02, даёт немедленную ценность на телефоне. Можно грилить сразу.
3. **AFTER pre-decision + shared-STT ADR**: concern 3 (голосовые инсайты) — дешевле concern 2, стоит на готовом insights-пути; хороший первый вертикальный срез общей STT-инфры.
4. **AFTER concern 3 (или параллельно после STT-ADR)**: concern 2 (AI-онбординг) — самый крупный, переиспользует STT-слой, доказанный на concern 3.

## Friction Points

- **Pre-decision (фазы)**: обе AI-фичи (2, 3) относятся к PHASE-02 «AI-инсайты (LLM orchestration layer)», который сейчас `planned`, а активна PHASE-01 (нативный iOS). Нужно явное решение пользователя: открыть PHASE-02 сейчас vs закончить iOS. По §1 CLAUDE.md «один тикет = один сеанс» — параллельно тянуть iOS и новую фазу рискованно.
- **Cross-cutting pre-decision (STT)**: concern 2 и concern 3 оба требуют «voice → text». Нужно решить один раз: Whisper vs облачный STT vs on-device iOS Speech, и общий ли это компонент. Это кандидат в отдельный ADR ДО обоих PRD.
- **Платформа ввода не определена**: дамп не говорит, голос — это web, iOS или оба. Concern 1 явно про web; AI-фичи (2,3) — платформа открыта. Если голос идёт на iOS, а редизайн — на web, фичи расходятся по кодовым базам.
- **Конфликт скоупа в concern 1**: «переосмыслить, где всё живёт» (крупный редизайн IA) против узкого «редактор категорий центрируется + карточки масштабируются» (квикфикс-уровень). Нужно выбрать границу до grill.
- **Не дублировать отгруженное**: часть пункта 1 уже сделана в PHASE-01/35 — грил concern 1 должен исключить эти квикфиксы, иначе повтор работы.

## Cross-cutting Notes

- **Общий STT-слой** проходит через concern 2 и 3 — сильный аргумент за один ADR «voice → text infra» перед обоими PRD; concern 3 — дешёвый первый потребитель для проверки.
- **Существующая insights-инфра**: `app/llm/` (client/context/prompts/cli) + insights API/CRUD/`ai_report` модель уже дают текстовый period-context путь. Concern 3 — расширение, не greenfield; concern 2 переиспользует тот же orchestration-слой (§4 CLAUDE.md: изоляция LLM обязательна).
- **Событийный нейминг**: любые новые события AI-фич должны следовать `<owner>.<entity>.<action>` (§4). Учесть на этапе PRD/ADR.

## Not tickets / ops follow-ups (не сайзим как concern)

- **Prod-редеплой + миграция**: на проде нужно `alembic upgrade head` (миграция `idempotency_key`) и редеплой фикса PHASE-01/35; до этого прод `GET /entries` отдаёт 500. Чисто серверное действие, не build-concern.
