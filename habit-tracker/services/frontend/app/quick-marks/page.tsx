'use client';
// [review:need-review] PHASE-03/125
// summary: /quick-marks — the desktop entry point; the screen itself is shared with the mobile twin, so this page only names it

import QuickMarksScreen from '@/components/QuickMarksScreen';

export default function QuickMarksPage() {
  return <QuickMarksScreen />;
}
