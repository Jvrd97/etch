'use client';
// [review:need-review] PHASE-03/139
// summary: /roles/rules — the desktop entry point; the screen itself is shared with the mobile twin, so this page only names it

import RoleRulesScreen from '@/components/roles/RoleRulesScreen';

export default function RoleRulesPage() {
  return <RoleRulesScreen />;
}
