'use client';
// [review:need-review] PHASE-03/134
// summary: /roles — the desktop entry point; the screen itself is shared with the mobile twin, so this page only names it

import RolesScreen from '@/components/roles/RolesScreen';

export default function RolesPage() {
  return <RolesScreen />;
}
