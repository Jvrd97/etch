# [review:need-review] PHASE-03/111, PHASE-03/113
# summary: the chat corner of the LLM layer — the streaming transport (client), the system prompt with its context version (prompt) and the bounded day card that goes under it (context)
# summary: the chat corner of the LLM layer — the streaming transport (client) and the system prompt with its context version (prompt)
